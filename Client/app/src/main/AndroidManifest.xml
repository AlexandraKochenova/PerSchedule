<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.client">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">

        </activity>

        <activity android:name=".activities.NewFeedActivity"></activity>
        <activity android:name=".activities.NewPetActivity"></activity>
        <activity android:name=".activities.NewProductActivity"></activity>
        <activity android:name=".activities.PetListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.PetProfileActivity"></activity>
        <activity android:name=".activities.ScheduleActivity"></activity>
    </application>

</manifest>